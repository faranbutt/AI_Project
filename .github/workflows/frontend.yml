name: Faran
on: 
    push: 
        branches: 
            - main
jobs:
    build-deploy:
        runs-on: ubuntu-latest
        steps:
            - name: Get Code
              run: actions/checkout@v4
            - name: list all
              working-directory: ./frontend 
              run: ls 
            - name: Run frontend
              run: npm run dev
              